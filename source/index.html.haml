---
title     : TISMatsuri4
bodyclass : index
layout    : layout
---

#main
  .text
    %section#section-detail.cols
      %h1 Details

      .col4
        %h2 What?
        %p
          TISMatsuriはTIS社員の社員による社員のための社内カンファレンスです。
        %p
          TISMatsuri is a company conference for employees by TIS employees.

      .col4
        %h2 Where?
        %p
          %a{href: "http://www.tis.co.jp/", target: "_blank"} TIS, Inc.
        %p
          〒160-0023
          %br
          東京都新宿区西新宿8-17-1
          %br
          住友不動産新宿グランドタワー 14F
        %p
          %a{href: "http://www.tis.co.jp/company/information/network/shinjuku.html", target: "_blank"} Map

      .col4
        %h2 When?
        %p
          12月15日
          %br
          19:00-(open 18:30)
          %br
          %br
          Mon, December 15
          %br
          at 19:00 (18:30 door open)

      .col4
        %h2 How?
        %p
          - data.contact.each do |c|
            ITHDの方は
            %a{href: "#{c.twitter}", target: "_blank"}
              @shiraco
            %br
            社内の方は
            %a{href: "#{c.url}", target: "_blank"}
              Copla
            %br
            お問い合わせ下さい。

    %section#section-time-table.cols
      %h1 Time table
      .col
        %ul.time-table-list
          - data.timetable.each do |tt|
            %li
              .head
                .time
                  = tt.time
                .speaker-name
                  = tt.speaker
              .title
                - if tt.url.present?
                  %a{href: "#{tt.url}", target: "_blank"}
                    #{tt.title}
                    %i.icon-external-link
                    %br
                    #{tt.body}
                - else
                  = tt.title
                  %br
                .body
                  = tt.body

    %section#section-speaker.cols
      %h1 Speaker

      %ul.speaker-list
        - data.speaker.each do |speaker|
          %li.speaker-list-item<>
            .speaker
              .avatar
                %img{ src: "#{speaker.avatar}", alt: 'Avatar'}
              .info
                .session
                  - if speaker.url.present?
                    %a{href: "#{speaker.url}", target: "_blank"}
                      #{speaker.title}
                      %i.icon-external-link
                  - else
                    = speaker.title
                .names
                  .fullname
                    = speaker.fullname
                  .sns
                    - if speaker.id.present?
                      %a{href: "https://twitter.com/#{speaker.id}", target: "_blank"}
                        %i.icon-twitter
                    - else
                .detail
                  = speaker.organization
                .profiles
                  = speaker.profile
    %section#section-lt.cols
      %h1 LT

      .col
        %p
          ビールと軽食を用意した上での懇親会LTを予定しています。
          %br
            ※順序、タイトル、概要は変更となる可能性があります。
        %ul.lt-list
          - data.lt.each do |lt|
            %li
              .head
                .order
                  = lt.order
                .speaker-name
                  - if lt.id.present?
                    %a{href: "https://twitter.com/#{lt.id}", target: "_blank"} @#{lt.id}
                  - elsif lt.name.present?
                    #{lt.name}
                  - else
              .title
                - if lt.url.present?
                  %a{href: "#{lt.url}", target: "_blank"}
                    #{lt.title}
                    %br
                    #{lt.body}
                - else
                  = lt.title
                  %br
                .body
                  = lt.body

    %section#section-team.cols
      %h1 Team

      %ul.team-list
        - data.team.each do |member|
          %li.team-list-item<>
            %a{href: "https://github.com/#{member.id}", target: "_blank"}
              %img{ src: "https://avatars2.githubusercontent.com/u//#{member.gravatar}?s=100", alt: 'Avatar' }
              %span.userid
                @#{member.id}

